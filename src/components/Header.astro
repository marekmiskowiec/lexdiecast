<header class="header">
  <div class="header__container">
    <i class="fas fa-car"></i>
    <h1 class="header__title">LexDiecast Collector</h1>
    <h2 class="header__subtitle">More than 1000 diecast cars in stock</h2>
  </div>
</header>

<style>
  .header {
    height: 100vh;
    position: relative;
    background-image: linear-gradient(rgba(0, 0, 0, 0.8), rgba(0, 0, 0, 0.8)),
      url("/images/header-bg.jpg");
    background-size: cover;
    background-position: center center;
    background-attachment: fixed;
  }

  .header__container {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 100%;
  }

  .header__title {
    color: var(--color-light);
    font-size: 6rem;
    margin: 4.8rem 0 0 0;
  }

  .header__title::after {
    content: "";
    position: absolute;
    width: 10rem;
    height: 1rem;
    background: var(--color-main);
    bottom: 4.8rem;
    left: 50%;
    transform: translateX(-50%);
  }

  .header__subtitle {
    color: var(--color-light);
    font-size: 1.8rem;
    margin: 2.4rem 0 0 0;
  }

  .header i {
    font-size: 3rem;
    background: var(--color-main);
    width: 6rem;
    height: 6rem;
    line-height: 6rem;
    color: var(--color-light);
    border-radius: 50%;
  }
</style>
